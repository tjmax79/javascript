// A program that count the number of character in a string

function countCharacters(s) {
    return s.length;
}

// Example usage
const inputString = "Hello, World!";
const characterCount = countCharacters(inputString);
console.log(characterCount);